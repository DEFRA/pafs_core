<% content_for :title, t(".heading") %>

<%= render partial: 'shared/single_numeric_field_form',
  locals: {
    project: @project,
    numeric_field: :carbon_cost_build,
    heading: t(".heading"),
    lede: t('.lede').html_safe,
    notes: t('.notes').html_safe,
    full_width: true,
    units: "metric tonnes",
    min: 0,
    step: 0.01
  }
%>
