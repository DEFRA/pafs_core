<h1 class="govuk-heading-l"><%= t('.heading') %></h1>
<p class="lede"><%= t('.lede') %></p>
<div class="panel">
  <span class="govuk-heading-l"><%= @project.reference_number %></span>
</div>
<p><%= t('.info') %></p>

<%= link_to t('continue_label'), pafs_core.project_step_path(id: @project.to_param, step: @project.step), class: "button" %>

