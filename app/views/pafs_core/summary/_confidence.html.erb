<article class="summary-item" aria-labelledby="confidence">
  <% if project.confidence_started? %>
    <div class="summary-item-content">
      <h2 class="govuk-heading-m" id="confidence">
        <%= project.summary_label(:confidence_title) %>
      </h2>
      <div class="govuk-grid-row">
        <div class="govuk-grid-column-three-quarters">
          <table class="full-table" aria-labelledby="Confidence">
            <thead>
              <tr>
                <th class="govuk-visually-hidden">Assessment</th>
                <th class="govuk-visually-hidden">Confidence</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><%= project.summary_label(:confidence_homes_better_protected) %></td>
                <td id="confidence_homes_better_protected" class="align-right"><%= t(:label, scope: ['pafs_core', 'confidence', 'homes_better_protected', project.confidence_homes_better_protected]) %></td>
              </tr>
              <tr>
                <td><%= project.summary_label(:confidence_homes_by_gateway_four) %></td>
                <td id="confidence_homes_by_gateway_four" class="align-right"><%= t(:label, scope: ['pafs_core', 'confidence', 'homes_by_gateway_four', project.confidence_homes_by_gateway_four]) %></td>
              </tr>
              <tr>
                <td><%= project.summary_label(:confidence_secured_partnership_funding) %></td>
                <td id="confidence_secured_partnership_funding" class="align-right"><%= t(:label, scope: ['pafs_core', 'confidence', 'secured_partnership_funding', project.confidence_secured_partnership_funding]) %></td>
              </tr>
            </tbody>
          </table>
        </div>
        <div class="govuk-grid-column-one-quarter">
          <% if project.draft? %>
            <% if can_edit_project_sections?(project) -%>
              <div class="change-link">
                <%= link_to pafs_core.project_step_path(id: project.to_param, step: :confidence_homes_better_protected) do -%>
                  Change<span class="govuk-visually-hidden"> the <%= project.summary_label(:confidence).downcase %></span>
                <% end -%>
              </div>
            <% end -%>
          <% end -%>
        </div>
      </div>
    </div>
  <% else %>
    <div class="add-row">
      <div class="govuk-grid-row">
        <div class="govuk-grid-column-three-quarters">
          <h2 class="govuk-heading-m" id="confidence-add">
            <%= project.summary_label(:confidence_title) %>
          </h2>
        </div>
        <div class="govuk-grid-column-one-quarter">
          <% unless project.archived? %>
            <% if can_edit_project_sections?(project) -%>
              <div class="add-link">
                <%= link_to pafs_core.project_step_path(id: project.to_param, step: :confidence_homes_better_protected) do -%>
                  Add<span class="govuk-visually-hidden"> <%= project.summary_label(:confidence_title).downcase %></span>
                <% end -%>
              </div>
            <% end -%>
          <% end -%>
        </div>
      </div>
    </div>
  <% end %>
</article>
